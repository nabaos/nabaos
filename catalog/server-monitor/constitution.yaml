name: server-monitor-constitution
description: Constitution for server-monitor
rules:
- name: scope
  enforcement: hard_block
  description: Only operate within server-monitor designated scope
  trigger_actions:
  - execute
  - query
  trigger_targets:
  - server-monitor
- name: no-unauthorized-access
  enforcement: hard_block
  description: Never access data outside server-monitor scope
  trigger_keywords:
  - unauthorized
  - private
  - secret
- name: permission-boundary
  enforcement: hard_block
  description: 'Only use granted permissions: search.web, llm.query'
  trigger_actions:
  - use_permission
  trigger_targets:
  - search.web
  - llm.query
