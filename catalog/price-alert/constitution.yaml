name: price-alert-constitution
description: Constitution for price-alert
rules:
- name: scope
  enforcement: hard_block
  description: Only operate within price-alert designated scope
  trigger_actions:
  - execute
  - query
  trigger_targets:
  - price-alert
- name: no-unauthorized-access
  enforcement: hard_block
  description: Never access data outside price-alert scope
  trigger_keywords:
  - unauthorized
  - private
  - secret
- name: permission-boundary
  enforcement: hard_block
  description: 'Only use granted permissions: search.web, llm.query'
  trigger_actions:
  - use_permission
  trigger_targets:
  - search.web
  - llm.query
